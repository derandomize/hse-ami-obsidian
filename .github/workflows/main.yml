name: Deploy from Obsidian notes

on:
  push:
    branches:
      - main

jobs:
  publish_job:
    runs-on: ubuntu-latest
    name: Publish Obsidian notes to Github pages
    steps:
      - uses: ./github/actions/publish.yml@master 
        with:
          vault-path: vault
          github-token: ${{ secrets.GITHUB_TOKEN }}
